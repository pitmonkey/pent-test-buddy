# Knowledge Base Index — {{ENGAGEMENT_NAME}}

Catalog of distilled artifacts. **Read/grep this file before running any scan** — if the answer
is already here, use it instead of re-running the tool. One row per distilled artifact, added by
the `kb-distill` skill.

| Date | Artifact | Source raw file | Host(s) | Summary |
|------|----------|-----------------|---------|---------|
