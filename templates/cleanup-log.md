# Cleanup / Artifact Log — {{ENGAGEMENT_NAME}}

Every artifact dropped on a target (uploaded files, shells, accounts, persistence, config
changes) is logged here the moment it is created, by the `cleanup-tracker` skill. The
engagement is not clean until every row is `removed`.

| Time (UTC) | Host / target | Type | Location | How to remove | Status |
|------------|---------------|------|----------|---------------|--------|
